import { EnduserfilterPipe } from './enduserfilter.pipe';

describe('EnduserfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnduserfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
